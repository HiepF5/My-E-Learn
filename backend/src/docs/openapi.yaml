openapi: 3.0.3
info:
  title: MY LEARN E API
  version: 1.0.0
  description: Backend API documentation for MY LEARN E.
x-json-convention:
  case: snake_case
  applies_to: request_body_and_response_body
  note: Clients (web-admin, mobile-app) should consume snake_case fields directly.
servers:
  - url: http://localhost:5000
tags:
  - name: Health
  - name: Auth
  - name: Topics
  - name: Vocabulary
  - name: Content
  - name: Review
  - name: Errors
  - name: AI
  - name: Streak
  - name: Weak words
  - name: Speaking
  - name: Writing
  - name: AI Feedback
paths:
  /health:
    get:
      tags: [Health]
      summary: Health check
      responses:
        "200":
          description: Server is running
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessHealth"

  /api/auth/register:
    post:
      tags: [Auth]
      summary: Register new user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterRequest"
      responses:
        "201":
          description: User registered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegisterResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "409":
          $ref: "#/components/responses/Conflict"

  /api/auth/login:
    post:
      tags: [Auth]
      summary: Login and get JWT token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Login successful
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/topics:
    get:
      tags: [Topics]
      summary: List topics
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Topic list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TopicListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [Topics]
      summary: Create topic
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TopicUpsertRequest"
      responses:
        "201":
          description: Topic created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TopicItemResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/topics/today:
    get:
      tags: [Topics]
      summary: Today mission (primary topic, plan counts, review due)
      description: >-
        Aggregates the rule-based today plan (same inputs as AI generate-today-plan) with live
        review-due count and error-notebook summary for mobile home and topic screens.
      security:
        - bearerAuth: []
      parameters:
        - name: daily_target_words
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 50
        - name: review_cap
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
      responses:
        "200":
          description: Today mission snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TodayMissionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/topics/{id}:
    get:
      tags: [Topics]
      summary: Get topic by id
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TopicId"
      responses:
        "200":
          description: Topic detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TopicItemResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags: [Topics]
      summary: Update topic by id
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TopicId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TopicUpsertRequest"
      responses:
        "200":
          description: Topic updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TopicItemResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [Topics]
      summary: Delete topic by id
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TopicId"
      responses:
        "200":
          description: Topic deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessMessageOnly"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/vocabulary:
    get:
      tags: [Vocabulary]
      summary: List vocabulary
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Vocabulary list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VocabularyListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [Vocabulary]
      summary: Create vocabulary
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VocabularyUpsertRequest"
      responses:
        "201":
          description: Vocabulary created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VocabularyItemResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/vocabulary/{id}:
    get:
      tags: [Vocabulary]
      summary: Get vocabulary by id
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/VocabularyId"
      responses:
        "200":
          description: Vocabulary detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VocabularyItemResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/vocabulary/{id}/collocations:
    get:
      tags: [Content]
      summary: List collocations for a vocabulary
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/VocabularyId"
      responses:
        "200":
          description: Collocations list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CollocationListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      tags: [Content]
      summary: Create collocation for a vocabulary
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/VocabularyId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CollocationRequest"
      responses:
        "201":
          description: Collocation created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CollocationItemResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/vocabulary/{id}/collocations/{collocationId}:
    delete:
      tags: [Content]
      summary: Delete collocation by id
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/VocabularyId"
        - $ref: "#/components/parameters/CollocationId"
      responses:
        "200":
          description: Collocation deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessMessageOnly"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/vocabulary/{id}/word-family:
    get:
      tags: [Content]
      summary: List word family for a vocabulary
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/VocabularyId"
      responses:
        "200":
          description: Word family list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WordFamilyListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      tags: [Content]
      summary: Create word family item for a vocabulary
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/VocabularyId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WordFamilyRequest"
      responses:
        "201":
          description: Word family item created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WordFamilyItemResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/vocabulary/{id}/word-family/{wordFamilyId}:
    delete:
      tags: [Content]
      summary: Delete word family item by id
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/VocabularyId"
        - $ref: "#/components/parameters/WordFamilyId"
      responses:
        "200":
          description: Word family item deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessMessageOnly"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags: [Vocabulary]
      summary: Update vocabulary by id
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/VocabularyId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VocabularyUpsertRequest"
      responses:
        "200":
          description: Vocabulary updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VocabularyItemResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [Vocabulary]
      summary: Delete vocabulary by id
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/VocabularyId"
      responses:
        "200":
          description: Vocabulary deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessMessageOnly"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/review/today:
    get:
      tags: [Review]
      summary: Review queue for today
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 30
          required: false
          description: >
            Daily cap for due review items. If omitted, server uses 30.
            Values outside 1..100 are clamped to the nearest valid value.
      responses:
        "200":
          description: Due review items ordered by wrong_count and next_review
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewTodayResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/review/submit:
    post:
      tags: [Review]
      summary: Submit review result
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReviewSubmitRequest"
      responses:
        "200":
          description: Review result applied and history created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewSubmitResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/review/touch/{wordId}:
    get:
      tags: [Review]
      summary: Get touch history for a word
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WordId"
      responses:
        "200":
          description: Touch history by user and word
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TouchHistoryResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      tags: [Review]
      summary: Update one touch step (1/2/3)
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WordId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TouchPatchRequest"
      responses:
        "200":
          description: Touch step updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TouchHistoryResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/errors:
    get:
      tags: [Errors]
      summary: List error notebook items
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Error notebook list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorNotebookListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [Errors]
      summary: Create error notebook item
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErrorNotebookRequest"
      responses:
        "201":
          description: Error notebook item created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorNotebookItemResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/errors/top-repeated:
    get:
      tags: [Errors]
      summary: Top repeated errors for dashboard
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 50
      responses:
        "200":
          description: Top repeated errors
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorNotebookListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/errors/{id}:
    put:
      tags: [Errors]
      summary: Update error notebook item
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ErrorId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErrorNotebookRequest"
      responses:
        "200":
          description: Error notebook item updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorNotebookItemResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [Errors]
      summary: Delete error notebook item
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ErrorId"
      responses:
        "200":
          description: Error notebook item deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessMessageOnly"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/errors/{id}/fixed:
    patch:
      tags: [Errors]
      summary: Update fixed status
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ErrorId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                fixed:
                  type: boolean
              required: [fixed]
      responses:
        "200":
          description: Fixed status updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorNotebookItemResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/errors/{id}/repeat:
    patch:
      tags: [Errors]
      summary: Increment repeat count
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ErrorId"
      responses:
        "200":
          description: Repeat count incremented
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorNotebookItemResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/errors/tags:
    get:
      tags: [Errors]
      summary: List available error tags
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Error tags list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorTagListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [Errors]
      summary: Create error tag
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tag_name:
                  type: string
              required: [tag_name]
      responses:
        "201":
          description: Error tag created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorTagItemResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/errors/{id}/tags:
    get:
      tags: [Errors]
      summary: Get tags mapped to one error item
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ErrorId"
      responses:
        "200":
          description: Error tag mapping
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorTagMapResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags: [Errors]
      summary: Replace tags for one error item
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ErrorId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tag_ids:
                  type: array
                  items:
                    type: integer
              required: [tag_ids]
      responses:
        "200":
          description: Error tag mapping updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorTagMapResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/ai/generate-today-plan:
    post:
      tags: [AI]
      summary: Generate today learning plan (rule-based, no LLM)
      description: >
        Uses priority scoring, false-master heuristic, and error-pattern weights
        from the user's notebook and review progress. Does not call external AI APIs.
      security:
        - bearerAuth: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GenerateTodayPlanRequest"
      responses:
        "200":
          description: Plan JSON with topic, counts, mistake_focus, weak_word_ids
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GenerateTodayPlanResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/ai/error-patterns:
    get:
      tags: [AI]
      summary: List aggregated error-type patterns (from error notebook submissions)
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        "200":
          description: Pattern rows ordered by error_count
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiErrorPatternListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/streak/current:
    get:
      tags: [Streak]
      summary: Get current and best streak
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Current streak summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StreakCurrentResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/streak/check-in:
    post:
      tags: [Streak]
      summary: Mark today's study check-in
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Check-in saved and streak recalculated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StreakCurrentResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/streak/heatmap:
    get:
      tags: [Streak]
      summary: Get streak heatmap activity by date
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: days
          required: false
          schema:
            type: integer
            minimum: 7
            maximum: 365
            default: 90
          description: Number of days to include in heatmap
      responses:
        "200":
          description: Heatmap activity list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StreakHeatmapResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/weak-words:
    get:
      tags: [Weak words]
      summary: List weak words (recomputed from progress and recent wrong answers)
      description: |
        Recomputes per-user weak scores from review progress (`wrong_count`, `correct_count`)
        and wrong answers in `review_history` for the last 14 days, persists snapshots to
        `weak_word_detector`, then returns the top items by score.
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 30
          description: Max number of weak words to return
      responses:
        "200":
          description: Weak words list ordered by weak_score descending
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeakWordsListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/speaking-records:
    get:
      tags: [Speaking]
      summary: List speaking records for current user
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 30
      responses:
        "200":
          description: Speaking records list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SpeakingRecordListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [Speaking]
      summary: Create a speaking record
      security:
        - bearerAuth: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SpeakingRecordCreateRequest"
      responses:
        "201":
          description: Speaking record created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SpeakingRecordItemResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/writing-records:
    get:
      tags: [Writing]
      summary: List writing records for current user
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 30
      responses:
        "200":
          description: Writing records list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WritingRecordListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [Writing]
      summary: Create a writing record
      security:
        - bearerAuth: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WritingRecordCreateRequest"
      responses:
        "201":
          description: Writing record created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WritingRecordItemResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/ai/feedback:
    get:
      tags: [AI Feedback]
      summary: List AI feedback records for current user
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 30
      responses:
        "200":
          description: AI feedback list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiFeedbackListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [AI Feedback]
      summary: Create an AI feedback item
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AiFeedbackCreateRequest"
      responses:
        "201":
          description: AI feedback created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiFeedbackItemResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "503":
          description: LLM generation requested but OPENAI_API_KEY is not configured

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  parameters:
    TopicId:
      name: id
      in: path
      required: true
      schema:
        type: integer
      description: Topic id
    VocabularyId:
      name: id
      in: path
      required: true
      schema:
        type: integer
      description: Vocabulary id
    CollocationId:
      name: collocationId
      in: path
      required: true
      schema:
        type: integer
      description: Collocation id
    WordFamilyId:
      name: wordFamilyId
      in: path
      required: true
      schema:
        type: integer
      description: Word family item id
    WordId:
      name: wordId
      in: path
      required: true
      schema:
        type: integer
      description: Word id
    ErrorId:
      name: id
      in: path
      required: true
      schema:
        type: integer
      description: Error notebook id

  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    NotFound:
      description: Not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Conflict:
      description: Conflict
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"

  schemas:
    StreakCurrent:
      type: object
      properties:
        current_streak:
          type: integer
        best_streak:
          type: integer
        last_study_date:
          type: string
          format: date
          nullable: true
      required: [current_streak, best_streak]

    StreakCurrentResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          $ref: "#/components/schemas/StreakCurrent"
        message:
          type: string
      required: [success, data, message]

    StreakHeatmapItem:
      type: object
      properties:
        study_date:
          type: string
          format: date
        did_study:
          type: boolean
      required: [study_date, did_study]

    StreakHeatmapResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: array
          items:
            $ref: "#/components/schemas/StreakHeatmapItem"
        message:
          type: string
      required: [success, data, message]

    WeakWordItem:
      type: object
      properties:
        word_id:
          type: integer
        weak_score:
          type: number
          format: float
        wrong_count_snapshot:
          type: integer
        correct_count_snapshot:
          type: integer
        recent_wrong_count:
          type: integer
          description: Wrong answers in review_history in the last 14 days
        last_computed_at:
          type: string
          format: date-time
      required:
        - word_id
        - weak_score
        - wrong_count_snapshot
        - correct_count_snapshot
        - recent_wrong_count
        - last_computed_at

    WeakWordsListResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: array
          items:
            $ref: "#/components/schemas/WeakWordItem"
        message:
          type: string
      required: [success, data, message]

    SpeakingRecordItem:
      type: object
      properties:
        id:
          type: integer
        user_id:
          type: integer
        word_id:
          type: integer
          nullable: true
        prompt_text:
          type: string
          nullable: true
        transcript_text:
          type: string
          nullable: true
        audio_url:
          type: string
          nullable: true
        score:
          type: number
          nullable: true
        reviewed_at:
          type: string
          format: date-time
          nullable: true
      required: [id, user_id]

    SpeakingRecordCreateRequest:
      type: object
      properties:
        word_id:
          type: integer
        prompt_text:
          type: string
        transcript_text:
          type: string
        audio_url:
          type: string
        score:
          type: number
        reviewed_at:
          type: string
          format: date-time

    SpeakingRecordItemResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          $ref: "#/components/schemas/SpeakingRecordItem"
        message:
          type: string
      required: [success, data, message]

    SpeakingRecordListResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: array
          items:
            $ref: "#/components/schemas/SpeakingRecordItem"
        message:
          type: string
      required: [success, data, message]

    WritingRecordItem:
      type: object
      properties:
        id:
          type: integer
        user_id:
          type: integer
        word_id:
          type: integer
          nullable: true
        prompt_text:
          type: string
          nullable: true
        written_text:
          type: string
          nullable: true
        corrected_text:
          type: string
          nullable: true
        score:
          type: number
          nullable: true
        reviewed_at:
          type: string
          format: date-time
          nullable: true
      required: [id, user_id]

    WritingRecordCreateRequest:
      type: object
      properties:
        word_id:
          type: integer
        prompt_text:
          type: string
        written_text:
          type: string
        corrected_text:
          type: string
        score:
          type: number
        reviewed_at:
          type: string
          format: date-time

    WritingRecordItemResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          $ref: "#/components/schemas/WritingRecordItem"
        message:
          type: string
      required: [success, data, message]

    WritingRecordListResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: array
          items:
            $ref: "#/components/schemas/WritingRecordItem"
        message:
          type: string
      required: [success, data, message]

    AiFeedbackItem:
      type: object
      properties:
        id:
          type: integer
        user_id:
          type: integer
        source_type:
          type: string
          enum: [speaking, writing, general]
        source_id:
          type: integer
          nullable: true
        feedback_text:
          type: string
        suggestions:
          type: string
          nullable: true
        model_name:
          type: string
          nullable: true
      required: [id, user_id, source_type, feedback_text]

    AiFeedbackCreateRequest:
      type: object
      description: >-
        Either provide feedback_text manually, or set generate_llm to true with learner_text to
        generate feedback via OpenAI (requires OPENAI_API_KEY on the server).
      properties:
        source_type:
          type: string
          enum: [speaking, writing, general]
        source_id:
          type: integer
        feedback_text:
          type: string
          description: Required when generate_llm is not true.
        suggestions:
          type: string
        model_name:
          type: string
        generate_llm:
          type: boolean
          description: When true, learner_text is sent to the LLM and feedback_text is generated.
        learner_text:
          type: string
          description: Learner content to review when generate_llm is true.

    AiFeedbackItemResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          $ref: "#/components/schemas/AiFeedbackItem"
        message:
          type: string
      required: [success, data, message]

    AiFeedbackListResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: array
          items:
            $ref: "#/components/schemas/AiFeedbackItem"
        message:
          type: string
      required: [success, data, message]

    AiErrorPatternItem:
      type: object
      properties:
        id:
          type: integer
        user_id:
          type: integer
        pattern_name:
          type: string
        error_count:
          type: integer
        last_detected_at:
          type: string
          format: date-time
          nullable: true
      required: [id, user_id, pattern_name, error_count]

    AiErrorPatternListResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: array
          items:
            $ref: "#/components/schemas/AiErrorPatternItem"
        message:
          type: string
      required: [success, data, message]

    GenerateTodayPlanRequest:
      type: object
      properties:
        daily_target_words:
          type: integer
          minimum: 1
          maximum: 50
          description: Target new words for the day (default 10 in engine)
        review_cap:
          type: integer
          minimum: 1
          maximum: 100
          description: Target size for review queue (aligns with GET /api/review/today limit)

    MistakeFocusItem:
      type: object
      properties:
        error_type:
          type: string
        weight:
          type: number
      required: [error_type, weight]

    TodayPlanMeta:
      type: object
      properties:
        rule_engine_version:
          type: string
        llm:
          type: boolean
      required: [rule_engine_version, llm]

    TodayPlanPayload:
      type: object
      properties:
        topic:
          type: object
          nullable: true
          properties:
            id:
              type: integer
            topic_name:
              type: string
            level:
              type: string
              nullable: true
        counts:
          type: object
          properties:
            new_words_target:
              type: integer
            review_queue_target:
              type: integer
            touch_focus:
              type: integer
        mistake_focus:
          type: array
          items:
            $ref: "#/components/schemas/MistakeFocusItem"
        weak_word_ids:
          type: array
          items:
            type: integer
        false_master_word_ids:
          type: array
          items:
            type: integer
        priority_preview:
          type: array
          items:
            type: object
            properties:
              word_id:
                type: integer
              priority_score:
                type: number
        meta:
          $ref: "#/components/schemas/TodayPlanMeta"

    GenerateTodayPlanData:
      type: object
      properties:
        generated_at:
          type: string
          format: date-time
        today_plan:
          $ref: "#/components/schemas/TodayPlanPayload"
      required: [generated_at, today_plan]

    GenerateTodayPlanResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          $ref: "#/components/schemas/GenerateTodayPlanData"
        message:
          type: string
      required: [success, data, message]

    TopicUpsertRequest:
      type: object
      properties:
        topic_name:
          type: string
        description:
          type: string
          nullable: true
        level:
          type: string
          nullable: true
      required: [topic_name]

    TopicItem:
      type: object
      properties:
        id:
          type: integer
        topic_name:
          type: string
        description:
          type: string
          nullable: true
        level:
          type: string
          nullable: true
      required: [id, topic_name]

    TopicItemResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          $ref: "#/components/schemas/TopicItem"
        message:
          type: string
      required: [success, data, message]

    TopicListResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: array
          items:
            $ref: "#/components/schemas/TopicItem"
        message:
          type: string
      required: [success, data, message]

    TodayMission:
      type: object
      properties:
        date:
          type: string
          example: "2026-03-28"
        generated_at:
          type: string
          format: date-time
        review_due_count:
          type: integer
        new_words_target:
          type: integer
          nullable: true
        review_queue_target:
          type: integer
          nullable: true
        touch_focus:
          type: integer
          nullable: true
        top_errors_count:
          type: integer
        today_plan:
          type: object
          description: Rule-based plan payload (topic, counts, weak_word_ids, etc.)
      required:
        - date
        - generated_at
        - review_due_count
        - top_errors_count
        - today_plan

    TodayMissionResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          $ref: "#/components/schemas/TodayMission"
        message:
          type: string
      required: [success, data, message]

    ErrorResponse:
      type: object
      properties:
        success:
          type: boolean
          example: false
        data:
          nullable: true
        message:
          type: string
      required: [success, data, message]

    SuccessMessageOnly:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          nullable: true
        message:
          type: string
      required: [success, data, message]

    SuccessArray:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: array
          items:
            type: object
        message:
          type: string
      required: [success, data, message]

    SuccessHealth:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: object
          properties:
            status:
              type: string
              example: ok
        message:
          type: string
      required: [success, data, message]

    RegisterRequest:
      type: object
      properties:
        username:
          type: string
        email:
          type: string
          format: email
        password:
          type: string
          format: password
      required: [username, password]

    LoginRequest:
      type: object
      properties:
        username:
          type: string
        email:
          type: string
          format: email
        password:
          type: string
          format: password
      required: [password]

    UserPublic:
      type: object
      properties:
        id:
          type: integer
        username:
          type: string
        email:
          type: string
          nullable: true
      required: [id, username]

    RegisterResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          $ref: "#/components/schemas/UserPublic"
        message:
          type: string
      required: [success, data, message]

    LoginData:
      type: object
      properties:
        token:
          type: string
        user:
          $ref: "#/components/schemas/UserPublic"
      required: [token, user]

    LoginResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          $ref: "#/components/schemas/LoginData"
        message:
          type: string
      required: [success, data, message]

    VocabularyUpsertRequest:
      type: object
      properties:
        word:
          type: string
        phonetic:
          type: string
        meaning:
          type: string
        example_sentence:
          type: string
        audio_url:
          type: string
        image_url:
          type: string
        difficulty:
          type: integer
          minimum: 1
          maximum: 5
        topic_ids:
          type: array
          items:
            type: integer
      required: [word]

    VocabularyItem:
      type: object
      properties:
        id:
          type: integer
        word:
          type: string
        phonetic:
          type: string
          nullable: true
        meaning:
          type: string
          nullable: true
        example_sentence:
          type: string
          nullable: true
        audio_url:
          type: string
          nullable: true
        image_url:
          type: string
          nullable: true
        difficulty:
          type: integer
        topic_ids:
          type: array
          items:
            type: integer
      required: [id, word, difficulty, topic_ids]

    VocabularyItemResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          $ref: "#/components/schemas/VocabularyItem"
        message:
          type: string
      required: [success, data, message]

    VocabularyListResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: array
          items:
            $ref: "#/components/schemas/VocabularyItem"
        message:
          type: string
      required: [success, data, message]

    CollocationRequest:
      type: object
      properties:
        collocation:
          type: string
        example:
          type: string
      required: [collocation]

    CollocationItem:
      type: object
      properties:
        id:
          type: integer
        word_id:
          type: integer
        collocation:
          type: string
        example:
          type: string
          nullable: true
      required: [id, word_id, collocation]

    CollocationItemResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          $ref: "#/components/schemas/CollocationItem"
        message:
          type: string
      required: [success, data, message]

    CollocationListResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: array
          items:
            $ref: "#/components/schemas/CollocationItem"
        message:
          type: string
      required: [success, data, message]

    WordFamilyRequest:
      type: object
      properties:
        related_word:
          type: string
        relation_type:
          type: string
      required: [related_word]

    WordFamilyItem:
      type: object
      properties:
        id:
          type: integer
        root_word_id:
          type: integer
        related_word:
          type: string
        relation_type:
          type: string
          nullable: true
      required: [id, root_word_id, related_word]

    WordFamilyItemResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          $ref: "#/components/schemas/WordFamilyItem"
        message:
          type: string
      required: [success, data, message]

    WordFamilyListResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: array
          items:
            $ref: "#/components/schemas/WordFamilyItem"
        message:
          type: string
      required: [success, data, message]

    ReviewProgressItem:
      type: object
      properties:
        id:
          type: integer
        user_id:
          type: integer
        word_id:
          type: integer
        level:
          type: integer
        ease_factor:
          type: number
        interval_days:
          type: integer
        next_review:
          type: string
          format: date-time
          nullable: true
        correct_count:
          type: integer
        wrong_count:
          type: integer
        last_review:
          type: string
          format: date-time
          nullable: true
        word:
          type: string
          nullable: true
          description: Vocabulary headword (included on GET /api/review/today for flashcard UI)
        meaning:
          type: string
          nullable: true
        example_sentence:
          type: string
          nullable: true
        phonetic:
          type: string
          nullable: true
        fake_known_count:
          type: integer
          description: Incremented when a correct review took longer than 6s (false-master signal)
      required:
        [id, user_id, word_id, level, ease_factor, interval_days, correct_count, wrong_count]

    ReviewTodayResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: array
          description: Review progress rows with optional vocabulary snapshot fields for each word
          items:
            $ref: "#/components/schemas/ReviewProgressItem"
        message:
          type: string
      required: [success, data, message]

    ReviewSubmitRequest:
      type: object
      properties:
        word_id:
          type: integer
        answer_result:
          type: boolean
        selected_word_id:
          type: integer
          description: >
            Optional. When `answer_result=false`, this is the word the learner selected
            (the "confused" word). Used for pair-confusion boosting in Phase K.
          nullable: true
        response_time_ms:
          type: integer
          description: >
            Optional response time in milliseconds.
            When provided for correct answers, server applies a small ease-factor
            adjustment (+0.03 for fast <= 2500ms, -0.03 for slow >= 15000ms).
        review_mode:
          type: string
          enum: [recognize, type, sentence, flashcard]
        rating:
          type: string
          enum: [Again, Hard, Good, Easy]
      required: [word_id, answer_result]

    ReviewSubmitResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          $ref: "#/components/schemas/ReviewProgressItem"
        message:
          type: string
      required: [success, data, message]

    TouchPatchRequest:
      type: object
      properties:
        touch_step:
          type: integer
          enum: [1, 2, 3]
        done:
          type: boolean
      required: [touch_step]

    TouchHistoryItem:
      type: object
      properties:
        id:
          type: integer
        user_id:
          type: integer
        word_id:
          type: integer
        touch1_done:
          type: boolean
        touch2_done:
          type: boolean
        touch3_done:
          type: boolean
        eligible_for_learned:
          type: boolean
      required:
        [
          id,
          user_id,
          word_id,
          touch1_done,
          touch2_done,
          touch3_done,
          eligible_for_learned,
        ]

    TouchHistoryResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          $ref: "#/components/schemas/TouchHistoryItem"
        message:
          type: string
      required: [success, data, message]

    ErrorNotebookRequest:
      type: object
      properties:
        error_type:
          type: string
        wrong_text:
          type: string
        corrected_text:
          type: string
        explanation:
          type: string
        repeat_count:
          type: integer
          minimum: 1
        fixed:
          type: boolean
      required: [wrong_text]

    ErrorNotebookItem:
      type: object
      properties:
        id:
          type: integer
        user_id:
          type: integer
        error_type:
          type: string
          nullable: true
        wrong_text:
          type: string
          nullable: true
        corrected_text:
          type: string
          nullable: true
        explanation:
          type: string
          nullable: true
        repeat_count:
          type: integer
        fixed:
          type: boolean
      required: [id, user_id, repeat_count, fixed]

    ErrorNotebookItemResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          $ref: "#/components/schemas/ErrorNotebookItem"
        message:
          type: string
      required: [success, data, message]

    ErrorNotebookListResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: array
          items:
            $ref: "#/components/schemas/ErrorNotebookItem"
        message:
          type: string
      required: [success, data, message]

    ErrorTagItem:
      type: object
      properties:
        id:
          type: integer
        tag_name:
          type: string
      required: [id, tag_name]

    ErrorTagItemResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          $ref: "#/components/schemas/ErrorTagItem"
        message:
          type: string
      required: [success, data, message]

    ErrorTagListResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: array
          items:
            $ref: "#/components/schemas/ErrorTagItem"
        message:
          type: string
      required: [success, data, message]

    ErrorTagMapItem:
      type: object
      properties:
        error_id:
          type: integer
        tag_ids:
          type: array
          items:
            type: integer
      required: [error_id, tag_ids]

    ErrorTagMapResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          $ref: "#/components/schemas/ErrorTagMapItem"
        message:
          type: string
      required: [success, data, message]
